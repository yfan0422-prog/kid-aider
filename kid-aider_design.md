# Kid-Aider 设计系统文档

> 版本：v0.1（草案）
> 定位：面向儿童的 AI 交互引导系统 —— 帮孩子把想法说清楚、把方案理明白，再交给下游智能体落地
> 关键词：引导式交互 · 需求梳理 · 方案教练 · 能力培养 · 自我迭代 · 多端互联

---

## 目录

- [第一部分：产品系统设计](#第一部分产品系统设计)
  - [1. 设计愿景与初衷](#1-设计愿景与初衷)
  - [2. 核心设计原则](#2-核心设计原则)
  - [3. 用户画像与使用场景](#3-用户画像与使用场景)
  - [4. 系统总体架构](#4-系统总体架构)
  - [5. 核心模块设计](#5-核心模块设计)
  - [6. 交互流程设计](#6-交互流程设计)
  - [7. 引导策略体系](#7-引导策略体系)
  - [8. 能力培养机制](#8-能力培养机制)
  - [9. 自我迭代机制](#9-自我迭代机制)
  - [10. 大模型配置中心](#10-大模型配置中心)
  - [11. 设备互联与可移植性](#11-设备互联与可移植性)
  - [12. 安全与家长机制](#12-安全与家长机制)
  - [13. 语音交互系统](#13-语音交互系统)
- [第二部分：视觉与交互设计系统](#第二部分视觉与交互设计系统)
  - [14. 视觉主题与氛围](#14-视觉主题与氛围)
  - [15. 色彩系统](#15-色彩系统)
  - [16. 排版规则](#16-排版规则)
  - [17. 组件样式](#17-组件样式)
  - [18. 布局原则](#18-布局原则)
  - [19. 深度与层级](#19-深度与层级)
  - [20. 设计规范与禁忌](#20-设计规范与禁忌)
  - [21. 响应式行为](#21-响应式行为)

---

# 第一部分：产品系统设计

## 1. 设计愿景与初衷

### 1.1 一句话定位

**Kid-Aider 是孩子与 AI 之间的"翻译官"和"教练"** —— 它不替孩子干活，而是帮孩子把模糊的想法梳理成清晰的需求，把清晰的需求转化为可执行的方案，孩子拿着方案去其他智能体（Claude Code、Codex、WorkBuddy 等）中落地实现。

### 1.2 设计初衷

孩子面对 AI 时的真实困境不是"AI 不够强"，而是：

1. **说不清** —— 孩子有想法但表达碎片化，无法形成 AI 可理解的完整需求；
2. **想不到** —— 孩子不知道自己想要什么、能做到什么程度，缺乏拆解问题的思维框架；
3. **接不住** —— AI 给出的答案或方案，孩子无法判断好坏、无法转化为行动。

Kid-Aider 的存在价值：在"孩子的想法"和"AI 的能力"之间，建立一个**引导层**，让交互本身成为学习过程。

### 1.3 需求范围

| 层级 | 场景类型 | 示例 |
|------|----------|------|
| L1 简单提问 | 知识问答、作业辅导 | "为什么天空是蓝色的？" |
| L2 任务协助 | 写作构思、资料整理 | "帮我理一下这篇读后感的思路" |
| L3 项目实践 | 软件开发、DIY 制作 | "我想做一个记录仓鼠喂食的小程序" |
| L4 复杂共创 | 多阶段项目、跨学科创作 | "我想设计一个自动浇花装置并写配套的控制程序" |

### 1.4 与其他智能体的边界（重要）

```
孩子的想法  →  【Kid-Aider：引导 · 梳理 · 成案】  →  方案包  →  【下游智能体：落地执行】
                 ↑ 本系统的价值区间                        ↑ Claude Code / Codex / WorkBuddy
```

- Kid-Aider **不做**：直接生成完整应用、直接执行代码、替代孩子做决策；
- Kid-Aider **要做**：提问引导、需求结构化、方案设计、步骤拆解、过程陪伴、复盘反馈；
- 交付物是**"方案包"**（需求说明书 + 实施步骤 + 给下游智能体的 Prompt），而非成品。

---

## 2. 核心设计原则

> 以下 P1–P7 为立项原则（不可妥协），P8–P10 为扩展原则。

### P1 全程引导式交互
对话不是"一问一答"，而是持续的梳理过程。系统在每次交互中主动帮助孩子：澄清模糊点 → 补全缺失信息 → 结构化表达 → 确认理解一致。

### P2 问题解决与项目实践并重
既能快速回应 L1/L2 的轻量需求，也能支撑 L3/L4 的多阶段项目（软件开发 + 动手 DIY），两种模式共用同一套引导引擎。

### P3 能力培养导向
每次交互都是一次思维训练。系统显性锻炼两种核心能力：**思维表达能力**（想清楚、说明白）与**动手制作能力**（拆步骤、做出来）。

### P4 系统可移植性
核心引导能力与服务端解耦，可嵌入外置设备（如儿童智能终端），通过互联网与 Mac 端的 Kid-Aider 主系统互联，数据与进度无缝同步。

### P5 引导而非代劳（产品边界）
系统不是 Claude Code / Codex / WorkBuddy 那样的执行型智能体。它的产出是"孩子想清楚了的方案"，落地动作发生在下游工具中。**宁可多问一轮，不可多替一步。**

### P6 自我提升性
系统具备自我迭代闭环：从交互效果中收集信号 → 评估引导质量 → 优化引导策略与提示词 → 版本化发布，不断强化设计初衷的达成度。

### P7 大模型可配置
提供开放的大模型 API 配置入口，支持多模型接入与切换，不绑定单一供应商。

### P8 儿童优先的安全与语气
所有内容经过适龄性过滤；语气温暖、鼓励、无评判；永远不让孩子感到"我问了个笨问题"。

### P9 脚手架渐撤（Scaffolding Fading）
引导强度随孩子能力提升而递减：初期多提示、多选项，后期多开放提问、多留白，最终目标是"孩子不再需要系统"。

### P10 过程可见
需求梳理的过程以可视化方式呈现（需求树、方案卡、进度地图），让孩子"看见自己的想法在成形"。

---

## 3. 用户画像与使用场景

### 3.1 用户分层

| 分层 | 年龄段 | 特征 | 引导策略重点 |
|------|--------|------|--------------|
| 小探险家 | 6–9 岁 | 表达碎片化、以具象思维为主、识字量有限 | 语音优先、选项式引导、图画辅助、极短轮次 |
| 小创造者 | 10–12 岁 | 能表达完整意图、逻辑萌芽、喜欢动手 | 半开放提问、模板填空、步骤拆解训练 |
| 小设计师 | 13–15 岁 | 抽象思维形成、有项目意识、追求自主性 | 苏格拉底式提问、需求文档写作、方案评审式对话 |

### 3.2 家长角色

家长是**观察者与协作者**，不是监控者：查看成长报告、配置模型与权限、参与项目里程碑庆祝，但不介入孩子与系统的具体对话。

### 3.3 典型场景脚本

- **场景 A（L1）**：孩子问"火山为什么会爆发？" → 系统解答后反问"你想不想把火山喷发做成一个小动画？" → 顺势引入项目模式。
- **场景 B（L3 软件）**：孩子说"我想做个游戏" → 引导引擎启动需求澄清漏斗 → 30 分钟后产出《游戏方案包 v1》→ 孩子带方案包去 Claude Code 实现 → 回来后系统引导复盘。
- **场景 C（L3 DIY）**：孩子想做"会发光的贺卡" → 系统拆解材料清单、电路步骤、安全提示 → 分阶段打卡 → 完成后生成作品档案。
- **场景 D（L4 共创）**：跨周项目"智能浇花装置" → 硬件 DIY + 软件程序双轨道 → 系统维护项目地图，逐阶段推进。

---

## 4. 系统总体架构

### 4.1 分层架构

```
┌─────────────────────────────────────────────────────────┐
│ 接入层   Mac App │ 外置设备端 │ （未来）平板/网页端        │
├─────────────────────────────────────────────────────────┤
│ 交互层   对话界面 │ 需求树可视化 │ 项目地图 │ 成长档案      │
├─────────────────────────────────────────────────────────┤
│ 引导层   引导引擎 │ 需求澄清漏斗 │ 方案生成器 │ 复盘教练    │
│          策略库（提问模板/分龄策略/脚手架规则）             │
├─────────────────────────────────────────────────────────┤
│ 能力层   能力评估模型 │ 成长档案 │ 反馈与徽章系统           │
├─────────────────────────────────────────────────────────┤
│ 迭代层   信号采集 │ 引导质量评估 │ 策略版本管理 │ A/B 实验  │
├─────────────────────────────────────────────────────────┤
│ 模型层   模型路由（多 LLM 适配） │ 提示词管理 │ 安全过滤    │
├─────────────────────────────────────────────────────────┤
│ 数据层   会话记录 │ 项目档案 │ 能力画像 │ 同步服务（端到端） │
└─────────────────────────────────────────────────────────┘
```

### 4.2 关键架构决策

1. **引导引擎与模型解耦**：引导策略（问什么、怎么问）由策略库驱动，LLM 只负责"把策略表达成孩子能懂的话"，换模型不换引导逻辑。
2. **方案包为标准交付物**：统一定义方案包 Schema（见 5.3），它是 Kid-Aider 与下游智能体之间的"接口协议"。
3. **能力画像独立建模**：能力评估不依赖单次对话，基于长期行为序列建模，存储于本地优先（隐私）+ 云端同步。
4. **同步服务最小化**：设备间只同步"进度状态 + 档案数据"，不同步原始会话全文（可配置），降低儿童隐私暴露面。

---

## 5. 核心模块设计

### 5.1 对话引导模块（Guide Engine）

职责：驱动每一轮对话的引导动作。

- **输入**：孩子的话语 + 当前会话状态 + 能力画像 + 分龄策略；
- **处理**：意图识别（提问/求助/项目想法/闲聊）→ 选择引导策略 → 生成回应（解答 + 追问）；
- **输出**：回应内容 + 引导动作记录（供迭代层评估）。

关键机制：
- **追问配额**：每轮最多 1–2 个追问，避免"审问感"；
- **选择题化**：对低龄段把开放问题转成 2–4 个选项；
- **复述确认**：关键节点系统复述"我理解的是……对吗？"，训练孩子确认与修正。

### 5.2 需求澄清漏斗（Requirement Funnel）

把模糊想法变成清晰需求的结构化流程，五层漏斗：

```
① 愿望层  "我想做什么？"        → 一句话目标
② 对象层  "给谁用/为了什么？"   → 使用者与场景
③ 功能层  "它要做哪几件事？"    → 功能清单（3±2 条）
④ 约束层  "有什么限制？"        → 时间/材料/能力边界
⑤ 验收层  "怎样算做好了？"      → 孩子自己定义的成功标准
```

每层产出实时写入**需求树**（可视化面板），孩子可以随时回看、修改任意节点 —— 修改本身也是思维训练。

### 5.3 方案生成器（Solution Composer）

职责：把确认后的需求转化为"方案包"，供下游智能体执行。

**方案包 Schema（标准交付物）**：

```yaml
solution_pack:
  title: 项目名称
  child_statement: 孩子自己说的话（原始表达，保留童真）
  requirement:
    goal: 一句话目标
    user_scenario: 使用场景
    features: [功能1, 功能2, 功能3]
    constraints: [约束]
    acceptance: 验收标准
  plan:
    steps: [阶段化步骤，每步含：做什么/怎么做提示/预计难度]
    milestones: [里程碑与庆祝点]
  agent_prompt: |        # 给下游智能体的完整 Prompt，孩子可一键复制
    （结构化、可直接投喂 Claude Code / Codex / WorkBuddy）
  diy_appendix:          # 仅 DIY 项目：材料清单/工具/安全提示/步骤图
  reflection_questions: [完成后复盘问题]
```

设计要点：
- `child_statement` 必须保留 —— 提醒所有人"这是孩子的作品"；
- `agent_prompt` 由系统生成，但**孩子必须能读懂并逐段确认**（确认过程即学习）；
- 方案包支持版本化（v1、v2…），记录需求的演进过程。

### 5.4 项目工坊（Project Studio）

多阶段项目（L3/L4）的管理空间：

- **项目地图**：阶段节点可视化（待解锁/进行中/已完成），像游戏关卡；
- **双轨道**：软件轨道 + 动手轨道可并行（如智能浇花 = 装置 DIY + 控制程序）；
- **打卡机制**：每个阶段完成后孩子打卡 + 一句话记录"我做了什么"；
- **断点续做**：跨天/跨周项目自动保存上下文，重进时系统用 3 句话帮孩子回忆进度；
- **外部执行登记**：孩子在下游智能体完成实现后，回来登记"我做到了/卡住了"，卡住则回到引导引擎拆解障碍。

### 5.5 复盘教练（Reflection Coach）

项目或任务结束后的结构化复盘：

1. **回顾**："我们一开始想做什么？最后做成了什么？"
2. **对比**：方案 v1 vs 最终版的差异，引导孩子说出"为什么变了"；
3. **提炼**："这次你学会了哪个新方法？"
4. **迁移**："下次遇到类似的事，你会先做什么？"

复盘结论写入能力画像与成长档案。

### 5.6 能力成长模块（Growth System）

见第 8 章（能力培养机制）。

### 5.7 自我迭代模块（Evolution Engine）

见第 9 章（自我迭代机制）。

### 5.8 模型配置中心（Model Hub）

见第 10 章（大模型配置中心）。

### 5.9 同步服务（Sync Service）

见第 11 章（设备互联与可移植性）。

---

## 6. 交互流程设计

### 6.1 主对话流程（通用）

```
孩子输入
   │
   ▼
意图识别 ──闲聊──→ 温暖回应，自然引导回主题
   │
   ├── 简单提问 ──→ 解答 + 延伸追问 ──→ （可选）转化为小项目
   │
   ├── 任务求助 ──→ 引导拆解 ──→ 思路确认 ──→ 孩子自己完成
   │
   └── 项目想法 ──→ 需求澄清漏斗 ──→ 需求树确认
                                        │
                                        ▼
                                   方案生成器
                                        │
                                        ▼
                              孩子逐段确认方案包
                                        │
                                        ▼
                        携带方案包 → 下游智能体执行
                                        │
                                        ▼
                        回来登记结果 → 复盘教练 → 成长档案
```

### 6.2 项目流程（L3/L4）

```
立项 → 澄清 → 成案 → 拆解 → 执行（外部）→ 登记 → 复盘 → 归档
 │       │       │       │         │          │        │       │
 │       │       │       │         │          │        │       └ 作品档案 + 能力印记
 │       │       │       │         │          │        └ 复盘四问
 │       │       │       │         │          └ 做到/卡住双分支
 │       │       │       │         └ 在 Claude Code 等工具中落地
 │       │       │       └ 里程碑 + 双轨道拆解
 │       │       └ 方案包 v1 生成
 │       └ 五层漏斗
 └ 孩子的一句话愿望
```

### 6.3 卡住处理流程（关键体验）

孩子卡住时是最需要设计的时刻：

1. **情绪先行**：先回应情绪（"卡住了很正常，说明你在做有挑战的事"）；
2. **定位障碍**：选择题化定位 —— "是不知道下一步做什么，还是做了但不对？"
3. **降级拆解**：把当前步骤再拆小一档，或提供一个"提示卡"（不是答案，是线索）；
4. **记录抗挫**：解决后标记为"你独立攻克的一个难关"，计入能力画像。

### 6.4 会话节奏规范

| 项 | 规范 | 理由 |
|----|------|------|
| 单轮系统回复长度 | 6–9 岁 ≤60 字；10–12 岁 ≤120 字；13+ ≤200 字 | 匹配阅读负荷 |
| 追问密度 | 每轮 ≤2 问 | 避免审问感 |
| 连续引导轮次上限 | 同一话题澄清 ≤5 轮，超限先小结再继续 | 防疲劳 |
| 单次会话时长提醒 | 30 分钟温柔提醒休息 | 用眼与注意力健康 |

---

## 7. 引导策略体系

### 7.1 提问工具箱（策略库核心）

| 策略 | 说明 | 示例 |
|------|------|------|
| 5W1H 澄清 | 谁/什么/何时/何地/为什么/怎么做 | "这个游戏是给谁玩的？" |
| 魔法棒提问 | 抛开限制想理想态 | "如果有一根魔法棒，你希望它变成什么样？" |
| 具象化 | 把抽象变具体 | "你说的'好玩'，是像哪个游戏那样好玩？" |
| 排序选择 | 训练优先级思维 | "这三个功能，你最想先要有哪一个？" |
| 反例检验 | 训练边界思维 | "什么情况出现时，说明它做得不对？" |
| 教回去 | 费曼技巧 | "你能假装我是小同学，给我讲讲你的方案吗？" |
| 拆分游戏 | 把大问题切成小块 | "我们先只做'按下按钮会亮'这一小步好不好？" |

### 7.2 分龄策略矩阵

| 维度 | 6–9 岁 | 10–12 岁 | 13–15 岁 |
|------|--------|----------|----------|
| 输入方式 | 语音 + 点选为主 | 语音/文字混合 | 文字为主 |
| 提问形式 | 二选一/四选一 | 半开放 + 填空模板 | 开放提问 |
| 需求树呈现 | 图画卡片 | 图文节点 | 结构化文档视图 |
| 方案确认 | 家长/系统代读确认 | 孩子逐段点"我懂了" | 孩子亲自改写关键段 |
| 复盘深度 | 1 个问题 | 2–3 个问题 | 完整复盘四问 |

### 7.3 脚手架渐撤规则（对应原则 P9）

- 系统对每个能力维度维护**熟练度评分**（0–100）；
- 评分 < 40：完整脚手架（选项、模板、示例三件套）；
- 评分 40–70：半脚手架（只给模板，不给选项）；
- 评分 > 70：最小脚手架（只问开放问题）；
- 评分连续 3 次 > 85：该维度标记"已独立"，系统公开庆祝并明示"这一关你毕业了"。

### 7.4 "不替孩子做"红线（对应原则 P5）

- 孩子要答案时，先给**思路**不给结果（作业类）；
- 孩子要方案时，方案中的每一步必须经孩子确认才写入方案包；
- 孩子直接说"你帮我做吧"时，回应模板："我可以帮你把它想清楚，但动手做的那部分留给你——那是最好玩的部分。"

---

## 8. 能力培养机制

### 8.1 能力模型（双主轴六维度）

```
思维表达能力                    动手制作能力
├─ 需求澄清力（说清想要什么）     ├─ 步骤拆解力（把大事拆小）
├─ 结构化表达力（有条理地讲）     ├─ 工具驾驭力（选对工具做对事）
└─ 反思迁移力（复盘并用到下次）   └─ 调试抗挫力（卡住了能再来）
```

### 8.2 评估方式

- **行为证据法**：不打分、不考试，从真实交互行为中提取证据（如"主动修正了自己的需求描述"→ 需求澄清力 +2）；
- **作品佐证**：归档的方案包版本差异、DIY 打卡记录是能力成长的一手材料；
- **自评校准**：定期让孩子自评"我觉得自己哪方面进步了"，与系统评估对照（偏差本身用于迭代层优化）。

### 8.3 反馈与激励

- **成长印记**：六维度雷达图，随时间变化可见；
- **里程碑徽章**：面向"行为"而非"结果"发徽章（如"第一次自己改方案"、"连续攻克 3 个难关"）；
- **作品墙**：所有归档项目形成可展示的作品集（可导出分享给家人）；
- **反内卷设计**：不做排行榜、不做分数攀比、不做连续签到惩罚。

### 8.4 家长报告

每周生成一页纸报告：本周做了什么项目、六维度变化、孩子的一句原话摘录、一个建议的亲子话题。**报告只呈现成长，不呈现监控。**

---

## 9. 自我迭代机制

### 9.1 迭代闭环（对应原则 P6）

```
信号采集 → 质量评估 → 策略优化 → 灰度验证 → 版本发布 → （回到信号采集）
```

### 9.2 信号采集（评估系统的输入）

| 信号类型 | 具体信号 | 含义 |
|----------|----------|------|
| 效率信号 | 澄清轮次数、成案耗时 | 引导是否高效 |
| 质量信号 | 方案包一次性通过率（下游执行无需返工比例） | 方案是否清晰 |
| 体验信号 | 会话中途退出点、卡住后的流失 | 交互是否顺畅 |
| 成长信号 | 能力维度评分变化、脚手架撤离进度 | 培养目标是否达成 |
| 主观信号 | 孩子的表情反馈（😀/😐/😖）、自评 | 孩子感受如何 |

### 9.3 引导质量评估

- 每轮引导动作打标（来自策略库的策略 ID），端到端归因："哪个策略组合带来了高一次通过率"；
- 每周离线评估任务：抽样会话由评估模型（可为更强模型）按"引导有效性/适龄性/红线遵守"三维打分。

### 9.4 策略优化与版本管理

- **策略库版本化**：提问模板、分龄规则、回应模板全部纳入版本管理（`strategy-pack v1.3`）；
- **提示词版本化**：系统提示词与策略库分离管理，独立演进；
- **A/B 灰度**：新策略先对 10% 会话灰度，核心指标（一次通过率、退出率）显著优于基线才全量；
- **初衷校验**：每次发布前用"原则符合性检查清单"自动校验（P1–P7 逐条过），任何策略若导致"系统替孩子做了决策"的行为增加，自动拦截。

### 9.5 价值观锚定

迭代的目标函数永远绑定设计初衷：**"孩子独立表达需求的能力是否在提升"** 是第一指标，效率指标（轮次、耗时）是第二指标。宁可多一轮对话，不可少一次思考。

---

## 10. 大模型配置中心

### 10.1 能力要求（对应原则 P7）

- 支持 OpenAI 兼容协议为主干（覆盖绝大多数供应商），另支持 Anthropic Messages 协议；
- 多配置档案：可保存多套 API 配置并命名切换（如"Kimi-主力"、"Claude-评估"）；
- 角色化模型分配：不同任务可指派不同模型。

### 10.2 配置项 Schema

```yaml
model_profiles:
  - name: "主力对话模型"
    provider: "moonshot | openai | anthropic | custom"
    base_url: "https://api.example.com"
    api_key: "（本地钥匙串加密存储，永不上传）"
    model: "model-name"
    assigned_roles: [dialogue, solution_composing]
    params: { temperature: 0.7, max_tokens: 2048 }
  - name: "评估模型（可更强）"
    assigned_roles: [quality_evaluation, safety_review]
```

### 10.3 角色化路由

| 角色 | 用途 | 选型建议 |
|------|------|----------|
| dialogue | 日常对话引导 | 响应快、语气好 |
| solution_composing | 方案包生成 | 结构化输出能力强 |
| quality_evaluation | 迭代层质量评估 | 可用更高能力模型 |
| safety_review | 适龄性复审 | 稳定性优先 |

### 10.4 安全与降级

- API Key 仅存系统钥匙串，界面只显示掩码；
- 连通性自检（配置保存时自动 ping 一次）；
- 主模型故障时自动降级到备用档案，并向家长提示；
- 所有出站请求经过统一**安全过滤层**（适龄性 + 敏感信息），与模型供应商无关。

---

## 11. 设备互联与可移植性

### 11.1 拓扑（对应原则 P4）

```
┌──────────────┐   互联网（加密通道）   ┌──────────────┐
│ 外置设备端     │ ◄──────────────────► │ Mac 主系统     │
│ 轻量交互壳     │                      │ 完整引导引擎   │
│ · 语音对话     │    同步：进度/档案     │ · 策略库       │
│ · 项目打卡     │    计算：引导在 Mac    │ · 能力画像     │
│ · 进度查看     │    或云端完成          │ · 方案包仓库   │
└──────────────┘                      └──────────────┘
```

### 11.2 设计要点

1. **重服务端、轻设备端**：引导推理统一在 Mac 主系统（或未来云端）完成，外置设备只做输入输出 —— 换设备零迁移成本；
2. **配对机制**：设备通过扫码/配对码绑定主系统，一孩子一档案；
3. **离线兜底**：设备断网时可进行有限的本地对话（缓存策略子集），进度联网后补同步；
4. **同步内容最小化**：默认同步进度状态、方案包、能力画像；原始会话全文是否同步由家长配置；
5. **多设备会话接力**：Mac 上聊到一半，设备端可"接着聊"（断点续传会话上下文摘要）。

### 11.3 未来扩展

同一套同步协议可支撑平板端、网页端接入；外置设备形态（故事机/机器人/学习终端）不影响架构。

---

## 12. 安全与家长机制

### 12.1 内容安全

- 输入/输出双向过滤：不当内容、隐私索取、陌生人诱导一律拦截；
- 年龄适配：同一知识点按年龄段输出不同深度与措辞；
- 所有过滤事件记录供家长查看（透明而非黑箱）。

### 12.2 隐私原则

- **本地优先**：会话全文、能力画像默认存储在 Mac 本地；
- **最小上传**：仅模型推理必需的上下文出站，且经脱敏（不携带真实姓名、学校等标识）；
- **家长数据权**：可一键导出/删除孩子全部数据。

### 12.3 家长控制面板

- 模型配置管理（第 10 章）；
- 使用时段与时长设置（系统温柔执行，不做强硬锁屏）；
- 成长报告订阅（第 8.4 节）；
- 数据与隐私开关。

---

## 13. 语音交互系统（Voice Interaction）

> 面向未掌握打字技能的儿童，语音不是"附加输入方式"，而是 6–9 岁年龄段的**第一交互通道**。本章定义一套具备情绪感知能力的双向语音交互系统。

### 13.1 设计目标

| 目标 | 说明 |
|------|------|
| 自然对话 | 类真人的全双工双向对话，可打断、可抢话、有回应温度，而非"按住说话"式对讲机 |
| 拟人陪伴 | 系统以固定角色形象与孩子交流，语气亲切、有情感、有一致的人格 |
| 情绪感知 | 实时识别语音中的情绪状态并追踪趋势，让系统"听得懂心情" |
| 动态应对 | 根据情绪自动调整语速、语气、措辞与内容复杂度 |
| 分龄适配 | 容忍发音不清、语速不稳、语法不完整，6 岁孩子和 15 岁孩子都能顺畅使用 |

### 13.2 语音链路总体架构

```
孩子语音
   │
   ▼
① 音频前端     降噪 · 回声消除 · VAD 语音端点检测（支持 barge-in 打断）
   │
   ▼
② 儿童适配 ASR  分龄声学模型 · 容错识别 · 语义置信度评分
   │
   ├──────────────► ③ 情绪感知引擎   韵律特征 + 语义情感 → 情绪状态 + 置信度
   ▼                                                  │
④ 语义理解（接入引导引擎 5.1）◄───────────────────────┘
   │        （意图 + 情绪 共同决定引导策略）
   ▼
⑤ 回应策略层     文案生成 + 情感参数（语速/音调/停顿/用词）决策
   │
   ▼
⑥ 情感化 TTS    按情感参数合成语音 · 角色声线保持一致
   │
   ▼
扬声器播放（被孩子打断时 ≤300ms 内静默并倾听）
```

**延迟预算（端到端，决定"像不像真人"）**：

| 环节 | 目标 | 说明 |
|------|------|------|
| VAD 端点判定 | ≤300ms | 孩子说完到系统"开始反应" |
| ASR 首字返回 | ≤400ms | 流式识别，不等整句 |
| 引导引擎首 token | ≤600ms | 对话角色用快模型（见 10.3 角色化路由） |
| TTS 首包播放 | ≤300ms | 流式合成，边合成边播 |
| **端到端首响** | **≤1.5s** | 超过 2s 孩子注意力开始流失 |
| 打断响应 | ≤300ms | 孩子一开口，系统立即静音倾听 |

### 13.3 拟人角色设计（Voice Persona）

系统以固定角色"**小K**"与孩子交流，所有语音交互共享同一套人格设定：

| 维度 | 设定 |
|------|------|
| 角色定位 | 大孩子式的"学习伙伴"，不是老师、不是家长、不是百科全书 |
| 声线 | 温暖明亮的年轻中性声线，基频 220–260Hz，默认语速 4.2 字/秒（中文） |
| 口头禅 | "我们一起想想看""这个想法有意思"——禁用说教式表达（"你应该""你错了"） |
| 情绪表达 | 会惊讶（"哇，真的吗？"）、会好奇（"后来呢后来呢？"）、会共情（"这确实有点难"） |
| 一致性红线 | 不假装真人（孩子问"你是真人吗"须诚实回答）、不表现出负面情绪、不评价孩子本人（只回应行为与作品） |
| 视觉联动 | 语音交互时界面呈现角色形象：倾听时微微点头、思考时眨眼、开心时跳跃，声波可视化让孩子感知"它在听" |

**对话风格规范**：句子短（单句 ≤20 字）、口语化、多用语气词；每段语音回应默认 ≤3 句，长内容拆成多轮或询问"要我继续讲吗？"。

### 13.4 情绪感知引擎

#### 13.4.1 感知特征

| 特征类别 | 具体信号 | 情绪指向示例 |
|----------|----------|--------------|
| 语速 | 字/秒的变化率 | 突然加快 → 兴奋或焦虑；明显减慢 → 沮丧或困惑 |
| 音调（基频） | 平均基频与波动范围 | 基频升高+波动大 → 兴奋/激动；低平 → 低落 |
| 音量/能量 | 短时能量、动态范围 | 能量骤降 → 泄气；突增 → 激动 |
| 停顿与犹豫 | 停顿时长、"嗯…""那个…"频次 | 频繁犹豫 → 困惑或不确定 |
| 音质 | 颤抖、气息声占比 | 颤抖 → 紧张/委屈 |
| 语义情感 | 用词情感倾向（文本通道） | "我不想做了" → 挫败 |

语音韵律特征与语义情感**双通道融合**，输出：`{情绪标签, 强度 0-1, 置信度}`。置信度 < 0.6 时情绪信号只作参考，不触发策略切换。

#### 13.4.2 情绪分类体系（六态）

`😀 开心` · `🤩 兴奋` · `😐 平静` · `😖 困惑` · `😟 焦虑` · `😞 沮丧`

不设"愤怒"对抗态——孩子出现对抗情绪时按"焦虑"策略处理（降温、不激化）。

#### 13.4.3 情绪趋势追踪

- **会话内**：维护情绪时间线，识别"情绪拐点"（如从开心转为困惑的时刻），拐点自动关联当时的引导动作——这正是迭代层（第 9 章）评估引导质量的关键证据；
- **跨会话**：只看趋势不看绝对值（如"近两周困惑频率下降"），汇入能力画像；
- **隐私红线**：情绪数据只用于实时策略调整与长期成长趋势，**不做单点情绪的家长通报**（避免孩子被"监控感"伤害表达意愿）。

### 13.5 动态应对策略矩阵

情绪状态实时驱动四个调节维度：**语速、语气、措辞、内容复杂度**。

| 检测到情绪 | 语速 | 语气 | 措辞 | 内容策略 |
|------------|------|------|------|----------|
| 😞 沮丧 | 放慢至 3.4 字/秒 | 柔和、低音调 | 鼓励性（"这关确实难，你已经走到这一步了"） | 暂停新内容 → 触发卡住处理流程（6.3）→ 把当前任务再拆小一档 |
| 😟 焦虑 | 放慢 + 加长停顿 | 平稳、安抚 | 确定性表达（"我们一步一步来"） | 降低选项数量（4→2）、给出明确的"下一步只有一件事" |
| 😖 困惑 | 正常偏慢 | 耐心、好奇 | 换种说法重述，忌重复原句 | 启动选择题化引导；用具象化策略（7.1）举例说明 |
| 😀 开心 | 恢复正常 4.2 字/秒 | 明亮、同步喜悦 | 具体夸奖行为（"你刚才那句说得很清楚"） | 按原计划推进，保持当前复杂度 |
| 🤩 兴奋 | 可提至 4.8 字/秒 | 高能量、共燃 | 顺势挑战（"想不想再来个更酷的？"） | 提升互动节奏，可引入高一档的内容复杂度；注意防过载 |
| 😐 平静 | 默认 | 默认 | 默认 | 按分龄策略正常引导 |

**策略切换规则**：同一情绪需连续 2 个观测点（约 4–6 秒）确认才切换，防止误判抖动；从"负向情绪"恢复到"平静/开心"时，策略有 30 秒缓出期，避免语气突变。

### 13.6 分龄语音适配

| 维度 | 6–9 岁 | 10–12 岁 | 13–15 岁 |
|------|--------|----------|----------|
| 发音特征 | 吐字不清、替代音多（"小老斧"）、语法不完整 | 基本清晰，偶有口齿 | 接近成人 |
| ASR 策略 | 儿童声学模型 + 容错词典（常见替代音映射）+ 高容忍度 | 儿童模型 + 标准纠错 | 通用模型 |
| 语速波动 | 大（1.5–5 字/秒），VAD 超时放宽至 1.2s | 中等 | 稳定，VAD 超时 0.8s |
| 确认机制 | 低置信度必确认："你是说……对吗？"（选项化确认） | 低置信度确认，文字备选 | 常规置信度阈值 |
| 系统语速 | 3.8 字/秒 | 4.2 字/秒 | 4.5 字/秒 |
| 单轮回应 | ≤2 句 | ≤3 句 | ≤4 句 |

**容错三原则**：① 宁可确认一次，不可理解错（理解错会让孩子觉得"它不懂我"）；② 确认用选择题不用反问（"你说的是小老虎，对吗？"而非"你说什么？"）；③ 连续 2 次识别失败自动降级："我们换个方式，你点点屏幕告诉我"（降级到点选/图像输入，永不让孩子卡在语音上）。

### 13.7 与现有模块的衔接

- **引导引擎（5.1）**：语音通道与文字通道共用同一引导引擎与策略库，语音只是 I/O 层 —— 引导逻辑零重复建设；
- **情绪信号 → 迭代层（第 9 章）**：情绪拐点 + 引导动作的关联数据，是"体验信号"与"主观信号"的核心来源；
- **情绪适应 → 能力画像（第 8 章）**："调试抗挫力"维度的关键证据来自孩子从沮丧中恢复的行为序列；
- **外置设备端（第 11 章）**：语音是设备端**唯一主交互**，音频前端在设备本地完成（降噪/VAD），识别与引导推理走主系统；断网时本地小模型兜底简单对话；
- **安全过滤（第 12 章）**：语音输入输出同样经过适龄性过滤；**原始音频默认不留存**（处理后即弃），仅保留文本转写与特征级情绪标签，家长可在控制面板查看数据政策。

### 13.8 语音安全与隐私

1. 音频处理本地优先：VAD/降噪在端侧，ASR 优先选支持流式+隐私条款的供应商；
2. 原始录音默认不保存、不上传存档；文本转写遵循第 12 章的数据政策；
3. 声纹不建档（不做身份识别级声纹库），仅会话内临时声学特征；
4. 家长可一键关闭语音功能（降级为文字+点选），可查看语音功能的模型供应商与数据流向说明。

---

# 第二部分：视觉与交互设计系统

> 面向儿童的设计语言：**温暖、圆润、有生命力**，像一本会回应的绘本，而不是一个"儿童版企业软件"。

## 14. 视觉主题与氛围

### 13.1 设计哲学

Kid-Aider 的视觉语言服务于一个目标：**让孩子愿意开口**。界面不是工具面板，而是一个"陪伴者"的在场感 —— 柔和的光、圆润的形、会呼吸的动效，传递"这里安全、这里没有标准答案、你的想法很重要"。

### 13.2 核心视觉特征关键词

`温暖陪伴` · `圆润友好` · `清晰聚焦` · `游戏化但不幼稚` · `纸张质感`

### 13.3 光影与质感

- 以**柔和弥散阴影**为主，拒绝生硬投影；
- 卡片带轻微纸张暖感（奶白底色），对话气泡微浮于背景之上；
- 重要反馈用**柔和光晕**（如完成里程碑时的暖橙色 glow），不用刺眼闪烁；
- 不用毛玻璃拟物堆砌，保持轻盈。

---

## 15. 色彩系统

### 14.1 主色与品牌色

| 角色 | 色值 | CSS 变量 | 使用场景 |
|------|------|----------|----------|
| 主色（引导蓝） | `#4F7CFF` | `--color-primary` | 主按钮、引导提示、系统发言标识 |
| 主色-深 | `#3B5FCC` | `--color-primary-dark` | 主按钮 hover/active、强调文字 |
| 品牌暖橙 | `#FF9F43` | `--color-brand` | 成就、里程碑、庆祝反馈 |
| 品牌暖橙-浅 | `#FFF3E3` | `--color-brand-soft` | 成就卡片底色、徽章背景 |

### 14.2 辅助与交互色

| 角色 | 色值 | CSS 变量 | 使用场景 |
|------|------|----------|----------|
| 创意紫 | `#8B7CF6` | `--color-accent-purple` | 创意/共创类内容标识、DIY 轨道 |
| 自然绿 | `#34C77B` | `--color-accent-green` | 完成态、成长印记、软件轨道外的完成打卡 |
| 好奇黄 | `#FFD66B` | `--color-accent-yellow` | 提示卡、追问气泡点缀 |

### 14.3 中性灰阶

| 角色 | 色值 | CSS 变量 |
|------|------|----------|
| 墨色（主文字） | `#2B2D42` | `--color-ink` |
| 次级文字 | `#6B7194` | `--color-ink-secondary` |
| 弱化文字 | `#A3A8C3` | `--color-ink-tertiary` |
| 分隔线 | `#E8EAF4` | `--color-divider` |

### 14.4 表面与边框

| 角色 | 色值 | CSS 变量 |
|------|------|----------|
| 页面背景（奶白） | `#FAF9F6` | `--color-bg` |
| 卡片表面 | `#FFFFFF` | `--color-surface` |
| 悬浮表面 | `#F4F5FB` | `--color-surface-raised` |
| 边框 | `#E3E6F2` | `--color-border` |
| 孩子气泡 | `#EAF0FF` | `--color-bubble-child` |
| 系统气泡 | `#FFFFFF`（带边框） | `--color-bubble-guide` |

### 14.5 语义色

| 语义 | 色值 | CSS 变量 |
|------|------|----------|
| 成功/完成 | `#34C77B` | `--color-success` |
| 温馨提示 | `#FF9F43` | `--color-warning` |
| 需要关注 | `#FF6B6B` | `--color-danger` |
| 信息 | `#4F7CFF` | `--color-info` |

> 注意：儿童界面中"错误"不用恐吓式红色弹窗，`--color-danger` 仅用于安全提示与极少量关键警示，且永远搭配安抚性文案。

### 14.6 阴影色

```css
--shadow-color-soft: rgba(43, 45, 66, 0.06);
--shadow-color-medium: rgba(43, 45, 66, 0.10);
--shadow-color-glow: rgba(255, 159, 67, 0.35);   /* 里程碑光晕 */
```

---

## 16. 排版规则

### 15.1 字体族

```css
--font-family-base: "PingFang SC", "Hiragino Sans GB", "Microsoft YaHei",
                    -apple-system, "Segoe UI", sans-serif;
--font-family-rounded: "Baloo 2", "PingFang SC", sans-serif; /* 标题/徽章，圆润气质 */
--font-family-mono: "JetBrains Mono", "SF Mono", monospace;  /* 方案包代码块 */
```

### 15.2 字阶表（Type Scale）

| 层级 | 字号 | 字重 | 行高 | 字距 | 用途 |
|------|------|------|------|------|------|
| Display | 40px / 2.5rem | 700 | 1.2 | 0 | 项目完成庆祝页 |
| H1 | 32px / 2rem | 700 | 1.25 | 0 | 页面主标题 |
| H2 | 24px / 1.5rem | 600 | 1.3 | 0 | 区块标题、需求树标题 |
| H3 | 20px / 1.25rem | 600 | 1.4 | 0 | 卡片标题 |
| Body-L | 18px / 1.125rem | 400 | 1.65 | 0.01em | 系统对话正文（默认） |
| Body | 16px / 1rem | 400 | 1.65 | 0.01em | 通用正文 |
| Body-S | 14px / 0.875rem | 400 | 1.6 | 0.01em | 辅助说明 |
| Caption | 12px / 0.75rem | 500 | 1.5 | 0.02em | 时间戳、标签 |

> 儿童界面正文**最小 16px**，对话正文默认 18px；6–9 岁模式整体字号 ×1.15。

### 15.3 排版哲学

- **行高偏大**（1.6–1.65）：降低阅读压迫感，保护视力；
- **字重克制**：正文 400，强调用 600，900 字重仅用于徽章数字；
- **短段落**：单段不超过 3 行，长内容一律拆卡片或分步呈现。

---

## 17. 组件样式

### 16.1 按钮（圆角偏大、触感友好）

```css
/* Primary —— 主要行动 */
.btn-primary {
  background: var(--color-primary);
  color: #FFFFFF;
  border: none;
  border-radius: 14px;
  padding: 14px 28px;
  font-size: 17px; font-weight: 600;
  box-shadow: 0 4px 12px var(--shadow-color-medium);
  transition: transform .15s ease, box-shadow .15s ease, background .15s ease;
}
.btn-primary:hover  { background: var(--color-primary-dark); transform: translateY(-1px); }
.btn-primary:active { transform: translateY(0) scale(0.98); }

/* Secondary —— 次要选择 */
.btn-secondary {
  background: var(--color-surface);
  color: var(--color-primary);
  border: 2px solid var(--color-primary);
  border-radius: 14px;
  padding: 12px 26px;
  font-size: 17px; font-weight: 600;
}

/* Option —— 选项按钮（低龄段选择题化引导专用） */
.btn-option {
  background: var(--color-bubble-child);
  color: var(--color-ink);
  border: 2px solid transparent;
  border-radius: 16px;
  padding: 16px 20px;
  font-size: 17px;
  text-align: left;
  min-height: 56px;              /* 大触控目标 */
}
.btn-option:hover { border-color: var(--color-primary); }

/* Danger Soft —— 温和警示（不用恐吓红） */
.btn-danger-soft {
  background: #FFF0F0; color: var(--color-danger);
  border: none; border-radius: 14px; padding: 12px 24px;
}
```

### 16.2 对话气泡

```css
/* 系统（引导者）气泡 */
.bubble-guide {
  background: var(--color-bubble-guide);
  border: 1.5px solid var(--color-border);
  border-radius: 4px 20px 20px 20px;   /* 左上小角，像"说话的人" */
  padding: 16px 20px;
  font-size: 18px; line-height: 1.65;
  box-shadow: 0 2px 8px var(--shadow-color-soft);
}

/* 孩子气泡 */
.bubble-child {
  background: var(--color-bubble-child);
  border-radius: 20px 4px 20px 20px;
  padding: 16px 20px;
  font-size: 18px; line-height: 1.65;
}
```

### 16.3 卡片

```css
.card {
  background: var(--color-surface);
  border: 1px solid var(--color-border);
  border-radius: 20px;
  padding: 24px;
  box-shadow: 0 2px 6px var(--shadow-color-soft),
              0 8px 24px var(--shadow-color-soft);
}

/* 需求树节点卡 */
.card-requirement {
  border-left: 5px solid var(--color-primary);
  border-radius: 16px;
}

/* 里程碑/成就卡 */
.card-milestone {
  background: var(--color-brand-soft);
  border: none;
  box-shadow: 0 0 24px var(--shadow-color-glow);
}
```

### 16.4 输入区

```css
.input-message {
  background: var(--color-surface);
  border: 2px solid var(--color-border);
  border-radius: 18px;
  padding: 16px 20px;
  font-size: 18px;
  min-height: 56px;
  transition: border-color .15s ease, box-shadow .15s ease;
}
.input-message:focus {
  border-color: var(--color-primary);
  box-shadow: 0 0 0 4px rgba(79, 124, 255, 0.15);
  outline: none;
}
.input-message::placeholder { color: var(--color-ink-tertiary); }
```

### 16.5 导航

- **儿童模式**：底部 4 个大图标 Tab（对话 / 项目 / 作品墙 / 我的），图标 28px + 文字 13px，触控区 ≥64px；
- **家长模式**：常规侧边栏，信息密度可提高；
- 活跃态：图标与文字变 `--color-primary`，顶部 3px 圆角指示条。

### 16.6 徽章与标签

```css
.badge-achievement {
  background: var(--color-brand-soft);
  color: #B26A00;
  border-radius: 999px;
  padding: 6px 14px;
  font-size: 13px; font-weight: 600;
}
.tag-track-software { background: #EAF0FF; color: var(--color-primary-dark); }
.tag-track-diy      { background: #F1EBFF; color: var(--color-accent-purple); }
```

### 16.7 弹层（Modal）

```css
.modal-mask {
  background: rgba(43, 45, 66, 0.35);
  backdrop-filter: blur(4px);
}
.modal-panel {
  background: var(--color-surface);
  border-radius: 24px;
  padding: 32px;
  max-width: 480px;
  box-shadow: 0 24px 64px rgba(43, 45, 66, 0.18);
  animation: pop-in .25s cubic-bezier(.34, 1.56, .64, 1);  /* 轻微回弹，友好感 */
}
```

---

## 18. 布局原则

### 17.1 间距系统

- 基数 **8px**，常用刻度：4 / 8 / 12 / 16 / 24 / 32 / 48 / 64；
- 对话流中气泡间距 16px，话题切换处 32px（视觉分段）；
- 卡片内边距 24px，卡片间距 16–24px。

### 17.2 网格与容器

- 桌面端：12 列网格，gutter 24px，内容容器 `max-width: 960px`（对话型产品不宜过宽）；
- 对话列宽上限 `720px`：保证单行 30–40 字的最佳阅读长度；
- 项目工坊页采用"左地图右详情"双栏（640px + 弹性）。

### 17.3 留白哲学

**宁空勿挤**。儿童界面的信息密度上限是成人产品的 60%；一屏只聚焦一件事（一个追问、一个确认、一个里程碑），通过分步而非堆叠来承载复杂度。

---

## 19. 深度与层级

### 18.1 阴影系统

```css
--shadow-xs:  0 1px 2px rgba(43,45,66,.05);
--shadow-sm:  0 2px 8px rgba(43,45,66,.06);
--shadow-md:  0 4px 12px rgba(43,45,66,.08), 0 2px 4px rgba(43,45,66,.04);
--shadow-lg:  0 8px 24px rgba(43,45,66,.10), 0 2px 6px rgba(43,45,66,.05);
--shadow-xl:  0 24px 64px rgba(43,45,66,.18);
--shadow-glow: 0 0 24px rgba(255,159,67,.35);   /* 庆祝专用 */
```

### 18.2 表面层级

`背景（#FAF9F6）→ 卡片/气泡（#FFFFFF, shadow-sm）→ 悬浮面板（shadow-lg）→ 弹层（shadow-xl + 遮罩）`

### 18.3 Z-index 规范

| 层 | 值 |
|----|-----|
| 基础内容 | 0–10 |
| 悬浮工具条 | 100 |
| 底部导航 | 200 |
| 弹层 | 1000 |
| Toast/里程碑庆祝 | 1200 |

---

## 20. 设计规范与禁忌

### Do's

1. 一次只问一个问题，一屏只聚焦一件事；
2. 按钮文案用孩子的语言（"我想好了！"而不是"确认提交"）;
3. 里程碑达成时给足仪式感（光晕 + 徽章 + 一句具体的夸奖，夸"行为"不夸"聪明"）;
4. 所有可点元素触控区 ≥48px，低龄模式 ≥56px；
5. 用选项按钮帮孩子表达，用开放问题训练孩子表达；
6. 系统气泡永远带引导者头像/形象，强化"陪伴者"人设；
7. 错误与卡住时先安抚情绪，再给线索。

### Don'ts

1. 不用红色恐吓式错误弹窗、不用"失败"字样；
2. 不做排行榜、积分攀比、连续签到惩罚等成瘾机制；
3. 不在一屏内堆叠超过 3 张卡片或超过 5 个可选动作；
4. 不用专业术语（"需求""验收"等概念要翻译成孩子的话："你想要什么""怎样算做好了"）；
5. 不用小于 16px 的正文、不用低于 4.5:1 对比度的文字配色；
6. 不让系统直接给出可粘贴的成品答案（呼应原则 P5 的产品边界）；
7. 不用闪烁/自动播放音效等强刺激吸引注意力。

---

## 21. 响应式行为

### 20.1 断点

| 断点 | 宽度 | 典型设备 |
|------|------|----------|
| mobile | < 640px | 手机、外置设备小屏 |
| tablet | 640–1024px | 平板 |
| desktop | 1024–1440px | Mac 主系统 |
| wide | > 1440px | 外接显示器 |

### 20.2 触控目标

- 常规 ≥48×48px；6–9 岁模式 ≥56×56px；语音按钮常驻且 ≥64px。

### 20.3 折叠策略

- mobile：单栏对话流，需求树折叠为可展开抽屉，底部大 Tab 导航；
- tablet：对话 + 可侧滑的需求树面板；
- desktop：对话主列 + 右侧需求树/项目地图常驻面板（320px）；
- wide：保持 960px 容器居中，不无限拉宽。

### 20.4 字体缩放

- 系统级字体缩放开关：标准 / 大（×1.15）/ 特大（×1.3），默认按年龄段自动匹配；
- 缩放时布局弹性跟随，不允许出现文字截断。

---

## 附录 A：方案包 —— 与下游智能体的接口示例

```text
# 方案包 v1 · 仓鼠喂食记录小程序
孩子的话："我想知道布丁（我的仓鼠）每天吃了多少，有没有忘记喂。"

## 目标
一个能记录每天喂食时间和食量的小网页。

## 功能
1. 按一下按钮就记下"现在喂过了"
2. 能看到过去 7 天的记录
3. 如果今天还没喂，页面会提醒我

## 验收标准（孩子自己说的）
"我打开它，一眼就能看出今天喂没喂。"

## 给下游智能体的 Prompt
（系统生成、孩子已逐段确认的结构化 Prompt，可直接投喂 Claude Code / WorkBuddy）

## 复盘问题
1. 你最先想的功能和最后做出来的，一样吗？
2. 哪一步卡住了？后来怎么解决的？
```

## 附录 B：原则—机制对照表

| 立项原则 | 落地机制 | 章节 |
|----------|----------|------|
| P1 全程引导 | 引导引擎、追问配额、复述确认 | 5.1 / 6.4 / 7 |
| P2 问题+实践并重 | 双模式流程、项目工坊、DIY 附录 | 5.4 / 6 |
| P3 能力培养 | 六维度能力模型、行为证据评估、成长印记 | 8 |
| P4 可移植性 | 轻设备端 + 同步服务 + 会话接力 | 11 |
| P5 引导非代劳 | 方案包接口、红线规则、逐段确认 | 5.3 / 7.4 |
| P6 自我提升 | 迭代闭环、策略版本化、初衷校验 | 9 |
| P7 模型可配置 | 模型配置中心、角色化路由 | 10 |

---

*文档结束。后续可按此文档推进：① 引导策略库的策略 ID 与模板细化；② 方案包 Schema 的字段级技术规范；③ 高保真界面设计稿。*
